<header class="page-heading">
    <a class="page-breadcrumb" href="{{ \App\Support\FrontendLocale::route('home') }}">{{ \App\Support\FrontendLocale::text('गृहपृष्ठ') }} <span aria-hidden="true">/</span></a>
    <span class="home-eyebrow">{{ $eyebrow }}</span>
    <h1>{{ $title }}</h1>
    <p>{{ $description }}</p>
</header>
